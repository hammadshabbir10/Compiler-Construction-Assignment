package Compiler;

public enum TokenType {
    PUNCTUATION,
    OPERATOR,
    KEYWORD,
    FLOAT,
    INTEGER,
    BOOLEAN,
    IDENTIFIER,
    CHARACTER,
    LITERAL,
    COMMENT,
    WHITESPACE,
    INVALID,
    STRING
}
